%table.table.table-condensed
  %thead
    %tr
      %th Name
      %th Date
      %th Seller
  %tbody
    - @purchased.each do |sale|
      %tr
        %td= link_to sale.book.name, pickup_path(guid: sale.guid)
        %td= time_ago_in_words(sale.created_at)
        %td= sale.seller_email
